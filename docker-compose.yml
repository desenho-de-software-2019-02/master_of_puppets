version: '3'
services:
  front:
    build: ./client
    ports:
      - 4200:4200
    volumes: 
      - ./client:/front
      - ./client/node_modules:/node_modules
    command: ng serve --host=0.0.0.0
